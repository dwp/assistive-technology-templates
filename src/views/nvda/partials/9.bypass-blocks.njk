{%- set observations -%}
<p>
  [ Enter observations ]
</p>
{%- endset -%}

{% set section = {
  title: 'Bypass blocks',
  id: 'bypass-blocks'
} %}

<section aria-describedby="{{section.id}}">
  <h2 id="{{section.id}}">
    {{section.title}}
  </h2>
  <p>
    Content which is repeated on multiple pages should have a mechanism which allows the user to skip past it. For example: a skip to main content link.
  </p>

  <h3 id="how-to-check-{{section.id}}">
    How to check<span class="at-visually-hidden">: {{section.title}}</span>
  </h3>
  <ol>
    <li>Press <span class="at-keys">CTRL + Home</span> to go to the top of the page</li>
    <li>Press <span class="at-keys">tab</span> and look for an element which skips over repetitive content such as navigation menus</li>
  </ol>
  
  <h3 id="what-to-check-{{section.id}}">
    What to check<span class="at-visually-hidden">: {{section.title}}</span>
  </h3>
  <p>
    Confirm that:
    <ul>
      <li>a skip link is present that links to the main content</li>
      <li>the skip link is the first interactive element on the page</li>
      <li>any other blocks of repetitive content are also skipped or have their own skip links</li>
    </ul>
  </p>

  <h3 id="observations-{{section.id}}">
    Observations<span class="at-visually-hidden">: {{section.title}}</span>
  </h3>
  {{- observations | safe -}}
</section>